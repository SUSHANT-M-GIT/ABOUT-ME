# Portfolio UI Update

The Let's Talk buttons now open the Contact section smoothly on desktop and mobile instead of relying on a configured email application. The email link remains available in the Contact section, and both CTA variants now have clear keyboard focus states.